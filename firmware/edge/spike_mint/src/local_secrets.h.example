// Copy to local_secrets.h (gitignored) and fill in real values from
// .local-info and web/.env.local before building this spike.
#pragma once

const char* kSsid = "YOUR_WIFI_SSID";
const char* kPassword = "YOUR_WIFI_PASSWORD";
const char* kMintHost = "europe-west1-alarm-system-100.cloudfunctions.net";
const char* kDeviceApiKey = "YOUR_DEVICE_API_KEY";
const char* kDatabaseUrl = "https://YOUR_PROJECT-default-rtdb.europe-west1.firebasedatabase.app";
// FirebaseClient's CustomToken auth flow calls Google's Identity Toolkit
// (signInWithCustomToken) under the hood, which needs a Web API key — same
// key as cloud_client.cpp's FIREBASE_WEB_API_KEY build flag. From Firebase
// console: Project settings -> General -> Web API Key.
const char* kFirebaseWebApiKey = "YOUR_FIREBASE_WEB_API_KEY";
